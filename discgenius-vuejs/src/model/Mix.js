export default class Mix {
    constructor(title, length, numSongs, tempo, id, progress) {
        this.title = title;
        this.length = length;
        this.numSongs = numSongs
        this.tempo = tempo;
        this.id = id;
        this.progress = progress;
    }
}